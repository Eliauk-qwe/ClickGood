1. 请求进入 Spring MVC

2. 根据路径 /user/login 找到 UserController.login()

3. Spring 从请求参数里取 userid=1

4. 转成 Long userId = 1L

5. Spring 自动传入 HttpServletRequest request

6. 执行 userService.getById(1L)

7. MyBatis-Plus 查询数据库 user 表

8. 返回 User 对象

9. 把 User 对象存入当前 Session

10. 用 ResultUtils.success(user) 包装响应

11. 返回 JSON 给前端
